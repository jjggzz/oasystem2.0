<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nnxy.jgz.oasystem.mapper.NoticeReadMapper">

    <insert id="insert" parameterType="NoticeRead">
        insert into t_notice_read (notice_id, user_id, read_time)
        values (#{notice.noticeId},#{user.userId},#{readTime})
    </insert>


</mapper>